---
name: daily-end
description: 每日下班前，與使用者同步當天的 Report。讀取今日的 To-do 記錄，比對當日 Git 變動並確認各項目的狀態，產出分為三層結構的 Report 報告。
---

# Daily End Skill (每日下班前同步)

此技能用於每日下班前，協助使用者同步今日的工作進度，並產出結構化、分為三層的 Report 報告。

## 執行流程與指令

當此技能被觸發時，請依序執行以下步驟：

### 1. 取得基礎資訊
- **日期**：獲取當前日期，格式為 `YYYYMMDD` (例如 `20260702`)。
- **使用者名稱**：預設為 `Woody`（可從 git config 中的 user.name 取得，或直接使用 `Woody`）。

### 2. 載入今日的 To-do 記錄
- 讀取 `daily_sync/[YYYYMMDD]_todo.md` 或 `[YYYYMMDD]_todo.json`。
- 如果找不到，請引導使用者提供今日任務清單。

### 3. 分析今日 Git 異動並對齊進度
- 檢查今日工作區變動或 commit 的程式碼，並逐一與使用者確認每個項目的完成狀態（如「完成」、「進行中」、「已取消」等）。

### 4. 生成並輸出三層結構的 Daily Report
將結果整理為以下三層結構。**注意：此輸出最後不要附上任何使用了哪些 Agent 技能或規定的備註。**

*   **第一層：回報事項** (給外人看的標準格式，列出所有任務與其完成狀態)
*   **第二層：來源事項** (同 Start，說明這些任務是在哪裡檢查到的)
*   **第三層：對齊時新加上** (記錄在對齊時新加上的任務之完成狀態)

```markdown
----下班前------
[使用者名稱] [YYYYMMDD] Report
-項目1 狀態
-項目2 狀態
-新項目A 狀態

來源事項：
- 項目1：來源說明 (檔案路徑/Commit)
- 項目2：來源說明

對齊時新加上：
- 新項目A：新加上的理由，狀態為 [狀態]
```

### 5. 保存記錄至回報資料夾
- 將上述三層結構的完整 Markdown 內容，保存至主專案根目錄的回報資料夾：
  `daily_sync/[YYYYMMDD]_report.md`。
